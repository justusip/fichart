// let newBoundTop = -Infinity;
// let newBoundBottom = Infinity;
// for (
//     let time = prevStep(boundLeft, timeStep);
//     time <= nextStep(boundRight, timeStep);
//     time += timeStep
// ) {
//     const interval = findData(time);
//     if (!interval)
//         continue;
//     if (interval.low < newBoundBottom)
//         newBoundBottom = interval.low - 1;
//     if (interval.high > newBoundTop)
//         newBoundTop = interval.high + 1;
// }
// boundTop = newBoundTop + 1;
// boundBottom = newBoundBottom - 1;
export default 1;
